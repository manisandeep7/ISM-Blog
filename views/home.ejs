<%- include('partials/header') -%>


<% for(var i=0;i<posts.length;i++) { %>
     

     <h2 class = "title"> <%= posts[i].title %> </h2>
     <p> <%= posts[i].des.substr(0,100) %>  
    <%    if(posts[i].des.length > 100 ) {  %> 
         ...
         <a href="/<%= posts[i].title %>" >Read More</a>
         
    <% } %>
    </p> 

    <% } %>

<%- include('partials/footer') -%>